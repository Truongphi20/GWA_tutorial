params {
    store_dir = "${STORE_DIR}"
}

process {
    executor       = "local"
    publishDir     = [
        path: { "output/" + task.process.replace(":", "/") },
        mode: "copy",
        enabled: true
    ]
}

docker {
    enabled = true
}

manifest.nextflowVersion = "25.10.0"