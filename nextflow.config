process {
    executor       = "local"
    publishDir     = [
        path: { "output/" + task.process.replace(":", "/") },
        mode: "copy",
        enabled: true
    ]
}

docker {
    enabled = true
}